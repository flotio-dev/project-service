POSTGRES_DB=projectdb
POSTGRES_USER=projectuser
POSTGRES_PASSWORD=projectpass
# When using devenv (local Postgres), prefer 5433 to avoid collision with Docker 5432
DB_PORT=5433

# DATABASE_URL est injecté au service app dans docker-compose
# Pour devenv (hors docker):
# DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5433/projectdb?sslmode=disable

# HTTP
PORT=8080

# Keycloak
KEYCLOAK_BASE_URL=http://localhost:8081/auth
KEYCLOAK_REALM=example
# Example environment variables for Docker Compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=projectdb
DB_PORT=5432
# Optional: app listen port
APP_PORT=8080
